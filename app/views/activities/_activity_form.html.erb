<%= form_for [@trip, @activity] do |f| %>
<div class = "form_style">
  <fieldset>
    <div>
      <%= f.hidden_field :trip_id , :value=>params[:trip_id] %>
    </div>

    <br><br>

    <div>
      <%= f.label :name %>
      <br>
      <%= f.text_field :name %>
    </div>

    <br><br>

    <div>
      <%= f.label :date %>
      <br>
      <%= date_select(:activity, :date, order: [:month, :day, :year]) %>
    </div>


    <br><br>

    <div>
      <%= f.label :description %>
      <br>
      <%= f.text_area :description %>
    </div>

    <br><br>

    <div>
      <%= f.label :location %>
      <br>
      <%= f.text_field :location %>
    </div>
  </fieldset>

    <br><br>

  <%= submit_tag "Submit", :class => "btn btn-large btn-primary" %>
<% end %>
</div>
